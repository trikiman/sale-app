# Plan 53-01: Repair Cart Truth Path

## Goal

Make the backend and cart client prefer truthful fast paths so MiniApp cart actions complete reliably in real use.

## Tasks

1. Stop inline stale-session refresh from consuming the cart hot path
2. Prefer direct VkusVill connectivity when healthy and keep a fallback transport
3. Add targeted regression coverage for stale session handling and transport fallback

## Acceptance Criteria

- [ ] Stale session metadata no longer forces a blocking refresh on every cart call
- [ ] Cart client falls back cleanly between direct and proxy transports
- [ ] Existing and new cart tests pass
