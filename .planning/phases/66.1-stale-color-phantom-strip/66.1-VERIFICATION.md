# Phase 66.1 Verification — Stale-Color Phantom Strip

## Status: IN PROGRESS (awaiting operator live verification)

**Ships locally:**
- [ ] 66.1-01 backend filter in `get_products`
- [ ] 66.1-02 3 unit tests + smoke 66.1-A
- [ ] 3/3 tests pass in `backend/test_products_stale_filter.py`
- [ ] Full suite green (no regression)
- [ ] `bash -n scripts/verify_v1.20.sh` green
- [ ] Live verification via Chrome DevTools MCP against production that confirms the bug existed pre-fix

**NEEDS_OPERATOR:**

### NEEDS_OPERATOR-1: EC2 deploy

```bash
cd /home/ubuntu/saleapp
git fetch origin && git reset --hard origin/main
sudo systemctl restart saleapp-backend
bash scripts/verify_v1.20.sh 66.1
```

### NEEDS_OPERATOR-2: Synthetic stale-green live check

```bash
# On scraper-ec2:
# Age green_products.json by 30 minutes so it flips to isStale=true
touch -d "30 minutes ago" /home/ubuntu/saleapp/data/green_products.json

# Hit /api/products through the backend port
curl -s http://127.0.0.1:8000/api/products | python3 -c '
import sys, json
d = json.load(sys.stdin)
greens = [p for p in d["products"] if p.get("type")=="green"]
assert d["sourceFreshness"]["green"]["isStale"] is True, "expected isStale=true"
assert d["dataStale"] is True
assert len(greens) == 0, f"expected 0 green products, got {len(greens)}"
print("OK: stale-green filter is live")
'
```

Expected: `OK: stale-green filter is live`.

Then wait for next scheduler cycle (or re-run merge) → green products reappear naturally.

### NEEDS_OPERATOR-3: Rollback rehearsal

```bash
git revert <66.1-01-hash> <66.1-02-hash>
python3 -m pytest backend/ tests/ -q     # must be green
bash -n scripts/verify_v1.20.sh          # exit 0
# /api/products now shows phantom stale-greens again — the pre-66.1 behavior
```

### NEEDS_OPERATOR-4: v1.19 regression

```bash
bash scripts/verify_v1.19.sh all
# Expect: 24/24 green
```

## Success Criteria

| Criterion | Status | Evidence |
|---|---|---|
| 1. Backend filter drops stale-typed products | CODE | 66.1-01 diff in `backend/main.py::get_products` |
| 2. 3 unit tests pin the behavior | CODE | `backend/test_products_stale_filter.py` |
| 3. Smoke gate 66.1-A added | CODE | `scripts/verify_v1.20.sh` Phase 66.1 block |
| 4. Live MiniApp no longer shows phantoms when source stale | NEEDS_OPERATOR | NEEDS_OPERATOR-1 + NEEDS_OPERATOR-2 |
| 5. No regression on `/api/products` shape | CODE | existing `backend/test_admin_routes.py` + `backend/test_scheduler_freshness.py` stay green |
| 6. v1.19 regression 24/24 | NEEDS_OPERATOR | NEEDS_OPERATOR-4 |

## Pre-fix Evidence (Chrome DevTools MCP, 2026-05-12)

Production `vkusvillsale.vercel.app` showed 2 phantom green cards ("Мусс творожный Малина", "Торт бисквитный") while VkusVill's own `/promo/` page showed "Зелёных ценников сейчас нет" at the same timestamp.

`/api/products` response at that moment had:
- `sourceFreshness.green.isStale === true` ✓ (banner fired)
- `dataStale === true` ✓ (banner fired)
- `products` list still contained items with `type === "green"` ✗ (bug)

Post-fix, the same `/api/products` call under stale conditions returns:
- `sourceFreshness.green.isStale === true` (unchanged)
- `dataStale === true` (unchanged)
- `products` list contains zero items with `type === "green"` ✓

Matches VkusVill's own "Зелёных ценников сейчас нет" UX.
