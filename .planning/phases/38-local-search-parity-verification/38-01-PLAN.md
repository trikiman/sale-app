---
phase: 38
plan: 01
title: "Parity Query Set And Local Search Verification"
objective: "Prove that newly backfilled products are now searchable locally and generate a repeatable parity/gap report."
wave: 1
depends_on: []
files_modified:
  - backend/catalog_parity_queries.json
  - verify_catalog_parity.py
  - backend/test_catalog_parity.py
requirements: [SRCH-04, SRCH-05, QA-02, OPS-01]
autonomous: true
---

# Plan 01: Parity Query Set And Local Search Verification

## Objective

Create a repeatable parity query set, generate a live-vs-local parity report, and add regression coverage proving newly backfilled products are searchable locally.

## must_haves

- [ ] Repeatable parity query set exists
- [ ] Live-vs-local parity report is generated
- [ ] Exact newly backfilled products are found locally
- [ ] Broad query gaps are visible rather than hidden

## Tasks

<task id="01" title="Create the parity query set and report script">
<read_first>
- .planning/phases/38-local-search-parity-verification/38-CONTEXT.md
- data/catalog_discovery_merged.json
- data/category_db.json
</read_first>

<action>
Create `backend/catalog_parity_queries.json` and `verify_catalog_parity.py` so Phase 38 can run a repeatable parity check.

1. Keep one broad query (`цезарь`) as a gap signal.
2. Add exact queries for newly backfilled discovery rows with expected product IDs.
3. Make `verify_catalog_parity.py` collect live search IDs and compare them to local History search IDs.
4. Write the result to `data/catalog_parity_report.json`.
</action>

<acceptance_criteria>
- `backend/catalog_parity_queries.json` exists
- `verify_catalog_parity.py` exists
- `verify_catalog_parity.py` writes `data/catalog_parity_report.json`
- exact queries include expected product IDs
</acceptance_criteria>
</task>

<task id="02" title="Add regression coverage for newly backfilled local search rows">
<read_first>
- backend/test_history_search.py
- backend/test_catalog_merge.py
</read_first>

<action>
Create `backend/test_catalog_parity.py` to prove that newly backfilled discovery rows with blank taxonomy can still be found locally by search.
</action>

<acceptance_criteria>
- `backend/test_catalog_parity.py` exists
- it proves blank-taxonomy discovery rows are searchable locally
- `pytest backend/test_catalog_merge.py backend/test_catalog_discovery.py backend/test_catalog_parity.py backend/test_history_search.py -q` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
pytest backend/test_catalog_parity.py -q
python verify_catalog_parity.py
```
