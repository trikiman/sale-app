---
phase: 45
plan: 03
title: "Visible Cart UX Verification Artifact"
objective: "Create a lightweight browser/manual verification path and final verification artifact for the bounded cart UX contract."
wave: 3
depends_on: [45-01, 45-02]
files_modified:
  - miniapp/test_ui.py
  - .planning/phases/45-cart-diagnostics-verification/45-VERIFICATION.md
requirements: [QA-04]
autonomous: true
---

# Plan 03: Visible Cart UX Verification Artifact

## Objective

Produce inspectable evidence that the visible cart UX now follows the intended bounded-latency contract, without building a large new E2E framework.

## must_haves

- [ ] There is a lightweight visible-flow sanity path for cart UX
- [ ] Verification artifact records the automated backend results and the browser/manual sanity observations
- [ ] The artifact is inspectable later during milestone completion

## Tasks

<task id="01" title="Adapt the lightweight UI verification helper for the new cart flow">
<read_first>
- miniapp/test_ui.py
- miniapp/src/App.jsx
- miniapp/src/ProductDetail.jsx
- .planning/phases/45-cart-diagnostics-verification/45-CONTEXT.md
</read_first>

<action>
Update `miniapp/test_ui.py` (or a sibling helper if cleaner) so the repo has a lightweight sanity path for the new cart UI:

1. Make the helper aware of the current add/cart surfaces instead of the old assumptions.
2. At minimum, verify the app loads and a product card/detail cart control is reachable.
3. Keep it lightweight and resilient; this is a sanity path, not a full formal E2E suite.
4. If live auth/cart interaction is not practical in the helper, record that limitation explicitly and keep the script focused on reachable visible states.
</action>

<acceptance_criteria>
- miniapp/test_ui.py is updated or replaced with a current cart-flow sanity helper
- the helper still runs without needing a brand-new E2E framework setup
- the helper covers app load plus at least one visible cart-control surface
</acceptance_criteria>
</task>

<task id="02" title="Write the final Phase 45 verification artifact">
<read_first>
- .planning/phases/45-cart-diagnostics-verification/45-CONTEXT.md
- backend/test_cart_items_fallback.py
- backend/test_cart_pending_contract.py
- backend/test_admin_routes.py
- miniapp/test_ui.py
</read_first>

<action>
After running the cart regression suite and the lightweight visible-flow sanity helper:

1. Write `45-VERIFICATION.md` capturing:
   - automated commands run
   - pass/fail outcomes
   - what the admin diagnostics surface now exposes
   - lightweight browser/manual sanity observations for the bounded cart UX
2. Record any residual risk clearly, especially where the lightweight browser/manual helper stops short of full live cart interaction.
</action>

<acceptance_criteria>
- `.planning/phases/45-cart-diagnostics-verification/45-VERIFICATION.md` exists
- the verification artifact lists the automated commands that were run and their outcomes
- the verification artifact includes cart diagnostics/admin observations and visible cart UX sanity notes
- residual risk is stated explicitly if the browser/manual helper remains lightweight
</acceptance_criteria>
</task>

## Verification

```bash
python -m py_compile miniapp/test_ui.py
test -f .planning/phases/45-cart-diagnostics-verification/45-VERIFICATION.md
```
