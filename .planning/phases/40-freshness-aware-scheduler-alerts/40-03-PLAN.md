---
phase: 40
plan: 03
title: "Freshness Regression Coverage"
objective: "Lock scheduler cadence, per-source freshness, and stale-warning behavior down with repeatable tests."
wave: 3
depends_on: [40-01, 40-02]
files_modified:
  - backend/test_scheduler_freshness.py
  - backend/test_admin_routes.py
requirements: [SCRP-10, SCRP-11, SCRP-12, OPS-03]
autonomous: true
---

# Plan 03: Freshness Regression Coverage

## Objective

Protect the new cadence and freshness contract with repeatable backend tests so stale warnings and green-first scheduling do not regress.

## must_haves

- [ ] Scheduler cadence logic is testable
- [ ] Per-source freshness reporting is tested
- [ ] Stale last-valid snapshot behavior is covered
- [ ] Admin route contract is protected

## Tasks

<task id="01" title="Add cadence and freshness unit coverage">
<read_first>
- scheduler_service.py
- backend/test_admin_routes.py
- .planning/phases/40-freshness-aware-scheduler-alerts/40-CONTEXT.md
</read_first>

<action>
Create `backend/test_scheduler_freshness.py` and extend existing admin tests so Phase 40 is regression-protected.

1. Extract or isolate any scheduler timing helpers needed so cadence decisions can be unit-tested without sleeping.
2. Add tests proving:
   - full cycles target 300 seconds
   - green-only runs target 60 seconds
   - full cycles win when another green run would make them late
3. Add tests proving the backend freshness payload marks colors stale after 10 minutes while still serving last valid data.
4. Extend `backend/test_admin_routes.py` to assert the new per-source freshness/admin contract.
</action>

<acceptance_criteria>
- backend/test_scheduler_freshness.py exists
- tests cover 300-second full-cycle cadence and 60-second green-only cadence
- tests cover full-cycle priority over extra green runs
- tests cover 10-minute stale thresholds while preserving last valid data
- `pytest backend/test_scheduler_freshness.py backend/test_admin_routes.py -q` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
pytest backend/test_scheduler_freshness.py backend/test_admin_routes.py -q
```
