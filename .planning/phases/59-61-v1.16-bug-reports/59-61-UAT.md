---
status: testing
phase: 59-61-v1.16-bug-reports
source: .planning/ROADMAP.md (v1.16 Success Criteria)
started: 2026-05-10T12:00:00Z
updated: 2026-05-10T14:25:00Z
note: Real UAT run using Chrome DevTools MCP + curl + local pytest + vite build. Production live on vkusvillsale.vercel.app + EC2 13.60.174.46. Findings below are from actual execution, not inferred.
---

## Current Test

[in-progress]

## Tests

### 1. Cold Start Smoke Test
expected: Stop any running backend, clear ephemeral state, start the app fresh. Backend boots without errors. /admin/status responds. data/bug_reports/ directory is either present or auto-created on first report. No missing dependency or import errors on startup.
result: skipped
reason: Local backend not started in this session. Did NOT cold-boot on EC2 either. Partial evidence: prod /api/auth/status returns 200 so some backend is up, but it is NOT the v1.16 revision (see tests 2-7, 14-19).

### 2. POST /api/bug-reports — happy path (authenticated, text only)
expected: Authenticated MiniApp user POSTs multipart form (text + meta JSON, no photo) with a valid X-Telegram-User-Id header. Response is 200 with success true and a report_id. A file data/bug_reports/<ISO-timestamp>_<random8>.json appears containing text, category, telegram_id, route, viewport, user_agent, app_version, timestamp, console_log_buffer.
result: issue
reported: "Production POST /api/bug-reports returns HTTP 404. Endpoint not deployed to EC2."
severity: blocker

### 3. POST /api/bug-reports — with photo attached
expected: Same as test 2 but with a small JPG/PNG attached (≤5MB, image/* mime). Response is 200, report_id returned, and a .jpg file is written alongside the .json with the same filename prefix.
result: blocked
blocked_by: server
reason: Cannot test — endpoint returns 404 in prod (see test 2).

### 4. POST /api/bug-reports — missing auth returns 401
expected: POST with no X-Telegram-User-Id header (or a bogus one) returns 401. No file is written to data/bug_reports/.
result: issue
reported: "Prod POST without auth returns HTTP 404 (endpoint missing), not 401."
severity: blocker

### 5. POST /api/bug-reports — oversized photo returns 400
expected: POST with a >5MB photo returns 400. No partial file written (no orphan .json or .jpg).
result: blocked
blocked_by: server
reason: Endpoint returns 404 in prod.

### 6. POST /api/bug-reports — non-image mime returns 400
expected: POST with a non-image file returns 400.
result: blocked
blocked_by: server
reason: Endpoint returns 404 in prod.

### 7. Backend pytest coverage (phase 59)
expected: pytest suite for bug-reports endpoint has tests covering success, missing auth, oversized photo, corrupt photo bytes, and no-photo case. All pass. Should be the 21/21 tests referenced in STATE.md.
result: pass
evidence: Ran `python -m pytest tests/test_bug_reports.py -v` locally at 2026-05-10 ~13:15 — 21 passed in 1.73s against HEAD (main.py rev 68623c3). Code is correct; it is just not deployed to prod.

### 8. MiniApp — bug-report entry point is discoverable
expected: From an authenticated MiniApp session there is a visible entry point (icon or menu item) that opens the bug-report form.
result: issue
reported: "Deployed Vercel bundle (index-DFayy0r0.js, 259876 bytes) does not contain BugReportPanel, setShowBugReport, consoleBuffer, or the bug emoji. Simulated authenticated state via fetch-hijack; header shows Logout/Cart/History/Theme/Admin but no bug-report pill. v1.16 frontend code is not deployed."
severity: blocker

### 9. MiniApp — form fields and client validation
expected: Form has text area (10-2000 chars), category select, photo input (≤5MB image/*), submit; client-side validation.
result: blocked
blocked_by: prior-phase
reason: Form component not present in deployed bundle (see test 8).

### 10. MiniApp — auto-attached runtime metadata
expected: Submit attaches route/URL, viewport, userAgent, build commit, telegram_id, timestamp.
result: blocked
blocked_by: prior-phase
reason: Form not deployed.

### 11. MiniApp — 30s console-log ring buffer attached
expected: App startup wraps console.error/warn/window.error into 30s/100-record ring buffer, attached on submit.
result: blocked
blocked_by: prior-phase
reason: consoleBuffer not present in deployed bundle.

### 12. MiniApp — success confirmation + failure preservation
expected: Success toast; failure preserves form contents for retry.
result: blocked
blocked_by: prior-phase
reason: Form not deployed.

### 13. MiniApp unit tests (phase 60) + vite build
expected: Frontend unit tests pass (should be 7/7), vite build green.
result: pass
evidence: Ran locally at 2026-05-10 ~13:10 — `node --test src/consoleBuffer.test.mjs` returned 7/7 pass. `npm run build` → "built in 624ms", dist/assets/index-DsukKGON.js produced. Local HEAD is correct; prod bundle is different/older (index-DFayy0r0.js).

### 14. GET /api/admin/bug-reports — populated state
expected: With at least one report saved, returns list with preview/has_photo/filename fields.
result: issue
reported: "Prod GET /api/admin/bug-reports returns HTTP 404 (with bad token too, so auth layer isn't even reached). Endpoint not deployed."
severity: blocker

### 15. GET /api/admin/bug-reports — empty state
expected: Empty list (not 404) when zero reports.
result: issue
reported: "Prod returns 404 regardless of state — endpoint missing."
severity: blocker

### 16. GET /api/admin/bug-reports — bad token returns 403
expected: Bad X-Admin-Token returns 403.
result: issue
reported: "Bad token returns 404 (endpoint not registered), not 403."
severity: blocker

### 17. /admin/status exposes bug-report counts
expected: /admin/status payload includes bug_reports_count and bug_reports_unread_count (or bugReports.{count,unread}).
result: issue
reported: "Cannot hit /admin/status directly (requires admin token, returns 403 to wrong tokens), but /api/products (served by same backend) exposes cartDiagnostics etc. and does NOT include any bugReports key. More critically: the v1.16 admin-status code in backend/main.py line 4450 does expose bugReports.count and unread in the committed HEAD — so the EC2 deployment predates that commit."
severity: blocker

### 18. Admin dashboard — Bug Reports (N) badge
expected: Existing admin dashboard surfaces a "Bug Reports (N)" badge driven by bug_reports_unread_count.
result: issue
reported: "backend/admin.html (local HEAD) has ZERO references to bugReports/bug_reports. Badge code was never written. This is a shipped v1.16 gap in Phase 61 success criterion #3 — backend exposes the count field but the UI never consumes it. Even if the backend is redeployed, the badge will not appear."
severity: major

### 19. Admin pytest coverage (phase 61)
expected: pytest covers /api/admin/bug-reports for empty/populated/bad-token cases.
result: pass
evidence: Included in the 21/21 pytest run in test 7 (test_admin_list_requires_token, test_admin_list_returns_empty_when_no_reports, test_admin_list_returns_recent_reports_with_preview).

### 20. Live EC2 end-to-end smoke
expected: Deployed MiniApp can submit a bug report that lands as a file on EC2, visible via /api/admin/bug-reports, increments /admin/status counts.
result: issue
reported: "End-to-end fails in three places at once: (a) Vercel frontend missing bug-report UI, (b) EC2 backend missing /api/bug-reports POST endpoint, (c) EC2 backend missing /api/admin/bug-reports GET endpoint. Nothing of v1.16 is actually shipped to prod."
severity: blocker

## Summary

total: 20
passed: 2
issues: 8
pending: 0
skipped: 1
blocked: 9

## Gaps

- truth: "POST /api/bug-reports returns 200 with report_id from an authenticated client"
  status: failed
  reason: "EC2 production backend returns HTTP 404 — the v1.16 endpoint commits (7f59f1a, 68623c3) were never deployed"
  severity: blocker
  test: 2, 4, 5, 6
  artifacts: ["EC2 /api/bug-reports returns 404", "/api/auth/status works (backend is alive, just old)", "local pytest 21/21 green at HEAD"]
  missing: ["EC2 redeploy of backend HEAD"]

- truth: "MiniApp shows a bug-report entry point for authenticated users"
  status: failed
  reason: "Deployed Vercel bundle index-DFayy0r0.js has no BugReportPanel / setShowBugReport / consoleBuffer / bug-emoji — the v1.16 MiniApp commits were never deployed to Vercel"
  severity: blocker
  test: 8, 9, 10, 11, 12
  artifacts: ["Chrome DevTools MCP: simulated authenticated header shows Logout/Cart/History/Theme/Admin only — no bug-report button", "string-search of deployed bundle returns no matches for BugReportPanel/setShowBugReport/consoleBuffer"]
  missing: ["Vercel redeploy of miniapp HEAD"]

- truth: "GET /api/admin/bug-reports returns list/403 (token-gated) with previews + has_photo + filename"
  status: failed
  reason: "EC2 backend returns 404 (endpoint not registered)"
  severity: blocker
  test: 14, 15, 16
  artifacts: ["curl returns HTTP 404 to both valid and invalid X-Admin-Token"]
  missing: ["EC2 redeploy"]

- truth: "/admin/status exposes bug_reports_count + bug_reports_unread_count"
  status: failed
  reason: "EC2 backend predates the backend/main.py changes that added the counters"
  severity: blocker
  test: 17
  artifacts: ["/api/products on same backend lacks bugReports key"]
  missing: ["EC2 redeploy"]

- truth: "Admin dashboard surfaces a Bug Reports (N) badge"
  status: failed
  reason: "backend/admin.html at HEAD has no references to bugReports/bug_reports. The UI code for the badge was never written — this is a gap in the committed work, not a deploy gap. Even after EC2 redeploys, the badge will not appear."
  severity: major
  test: 18
  artifacts: ["grep bugReports|bug_reports in backend/admin.html returned 0 hits"]
  missing: ["admin.html needs ~15-25 lines to read bugReports.unread from /admin/status and render a header badge. Pattern exists already: proxy-badge, cart-pending-count."]

## Out-of-scope observations (scheduler regression, separate phase)

**SCHEDULER DEAD — 4+ DAYS STALE.** /api/products shows:
- updatedAt: 2026-05-06 10:19:42
- ageMinutes: 6165.5 (approx 102 hours, approx 4.3 days)
- green / red / yellow: all status "stale"

The MiniApp's "Обновлено: 10:19" displayed over real-time ~18:54 is correct — it is 2026-05-06 at 10:19, not today. The stale banner is firing correctly; the underlying data production is what is broken. Silently broken since May 6.

This is unrelated to v1.16 content but blocks any live verification of v1.16 over a real fresh-data flow. Needs its own investigation phase (SSH to EC2, check saleapp-scheduler systemd unit, logs/scheduler.log, saleapp-xray.service, proxy pool).
