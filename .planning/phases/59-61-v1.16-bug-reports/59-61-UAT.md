---
status: complete
phase: 59-61-v1.16-bug-reports
source: .planning/ROADMAP.md (v1.16 Success Criteria)
started: 2026-05-10T12:00:00Z
updated: 2026-05-10T12:10:00Z
note: Bulk-completed by user after review — relies on autonomous-run evidence (21/21 backend pytest, 7/7 MiniApp unit tests, vite build green) recorded in STATE.md on 2026-04-28. Live EC2 smoke (test 20) accepted on user sign-off, not rerun in this session.
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Stop any running backend, clear ephemeral state, start the app fresh. Backend boots without errors. /admin/status responds. data/bug_reports/ directory is either present or auto-created on first report. No missing dependency or import errors on startup.
result: pass

### 2. POST /api/bug-reports — happy path (authenticated, text only)
expected: Authenticated MiniApp user POSTs multipart form (text + meta JSON, no photo) with a valid X-Telegram-User-Id header. Response is 200 with success true and a report_id. A file data/bug_reports/<ISO-timestamp>_<random8>.json appears containing text, category, telegram_id, route, viewport, user_agent, app_version, timestamp, console_log_buffer.
result: pass

### 3. POST /api/bug-reports — with photo attached
expected: Same as test 2 but with a small JPG/PNG attached (≤5MB, image/* mime). Response is 200, report_id returned, and a .jpg file is written alongside the .json with the same filename prefix.
result: pass

### 4. POST /api/bug-reports — missing auth returns 401
expected: POST with no X-Telegram-User-Id header (or a bogus one) returns 401. No file is written to data/bug_reports/.
result: pass

### 5. POST /api/bug-reports — oversized photo returns 400
expected: POST with a >5MB photo returns 400. No partial file written (no orphan .json or .jpg).
result: pass

### 6. POST /api/bug-reports — non-image mime returns 400
expected: POST with a non-image file (e.g. a .txt or .pdf uploaded as photo) returns 400. No partial file written.
result: pass

### 7. Backend pytest coverage (phase 59)
expected: pytest suite for bug-reports endpoint has tests covering success, missing auth, oversized photo, corrupt photo bytes, and no-photo case. All pass. Should be the 21/21 tests referenced in STATE.md.
result: pass

### 8. MiniApp — bug-report entry point is discoverable
expected: From an authenticated MiniApp session there is a visible entry point (icon or menu item) that opens the bug-report form. User can reach it without dev tools.
result: pass

### 9. MiniApp — form fields and client validation
expected: Form has a text area (10-2000 chars), category select (cart / login / scrape / ui / other), photo input (accept image/* up to 5MB), submit button. Client validates before sending (empty text blocked, too-long text blocked, too-big photo blocked).
result: pass

### 10. MiniApp — auto-attached runtime metadata
expected: On submit the form auto-attaches current route/URL, viewport (innerWidth × innerHeight), navigator.userAgent, build commit hash, telegram_id, ISO-8601 timestamp. Visible in the saved .json on the server.
result: pass

### 11. MiniApp — 30s console-log ring buffer attached
expected: App startup installs a console wrapper capturing console.error, console.warn, and window.error events into a ring buffer of last 30s (cap about 100 records). Buffer is included in the saved .json as console_log_buffer and contains real entries (trigger some errors before submitting to confirm).
result: pass

### 12. MiniApp — success confirmation + failure preservation
expected: Successful submission shows a confirmation toast. Simulated failure (kill backend / disconnect) shows actionable error message and keeps form contents intact for retry.
result: pass

### 13. MiniApp unit tests (phase 60)
expected: Frontend unit tests for the bug-report feature pass. Should be the 7/7 referenced in STATE.md. vite build is green.
result: pass

### 14. GET /api/admin/bug-reports — populated state
expected: With at least one report saved, GET /api/admin/bug-reports with valid X-Admin-Token returns JSON list containing per-report timestamp, telegram_id, category, text preview (first 200 chars), has_photo, filename.
result: pass

### 15. GET /api/admin/bug-reports — empty state
expected: With zero reports saved, GET /api/admin/bug-reports with valid X-Admin-Token returns an empty list (not a 404, not a 500). Graceful empty response.
result: pass

### 16. GET /api/admin/bug-reports — bad token returns 403
expected: GET /api/admin/bug-reports with missing or wrong X-Admin-Token returns 403. No data leaked.
result: pass

### 17. /admin/status exposes bug-report counts
expected: /admin/status payload includes bug_reports_count (total saved) and bug_reports_unread_count (unseen since last admin check). Counts update as new reports arrive and decrement when admin reads them.
result: pass

### 18. Admin dashboard — Bug Reports (N) badge
expected: Existing admin dashboard surfaces a Bug Reports (N) badge driven by bug_reports_unread_count. Badge appears and disappears as the unread count changes.
result: pass

### 19. Admin pytest coverage (phase 61)
expected: pytest covers /api/admin/bug-reports for empty state (0 reports), populated state (3+ reports), bad token returns 403. All pass.
result: pass

### 20. Live EC2 end-to-end smoke
expected: On EC2 (ubuntu@13.60.174.46), the deployed MiniApp can submit a bug report that lands as a file in data/bug_reports/ on the server, is visible via /api/admin/bug-reports, and increments /admin/status counts. This is the live verification STATE.md asks for before closing v1.16.
result: pass

## Summary

total: 20
passed: 20
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
