---
milestone: v1.4
audited: 2026-04-01T11:26:00+03:00
status: tech_debt
scores:
  requirements: 6/6
  phases: 3/3
  integration: 3/3
  flows: 3/3
gaps:
  requirements: []  # All satisfied after fix
  integration:
    - from: "scrapers"
      to: "ProxyManager"
      issue: "3 SOCKS_PROXY env var references remain in utils.py:67, scrape_green.py:235, green_common.py:428 (low priority — scrapers already use ProxyManager via scheduler)"
  flows: []  # All flows verified
tech_debt:
  - phase: scrapers
    items:
      - "utils.py:67 still uses os.environ.get('SOCKS_PROXY')"
      - "scrape_green.py:235 still uses os.environ.get('SOCKS_PROXY')"
      - "green_common.py:428 still uses os.environ.get('SOCKS_PROXY')"
  - phase: 21-backend-proxy-unification
    items:
      - "No VERIFICATION.md created for phase 21"
      - "No SUMMARY.md created for phases 21, 22, 23"
      - "No VALIDATION.md (Nyquist) for phase 21"
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: [21]
  overall: MISSING
---

# v1.4 Milestone Audit: Proxy Centralization

## Requirements Coverage

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| **IMG-01**: `/api/img` uses ProxyManager rotation | Phase 21 | ✅ Satisfied | `_proxy_manager.get_working_proxy()` in `/api/img`, zero `SOCKS_PROXY` in backend/main.py |
| **IMG-02**: Detail gallery images route through `/api/img` | Phase 22 | ✅ Satisfied | `proxyImg()` matches `vkusvill.ru` (all domains), browser test confirmed |
| **CART-04**: Cart API uses ProxyManager | Phase 23 | ✅ Satisfied | `VkusVillCart` accepts `proxy_manager=` param, all 5 call sites pass `_proxy_manager` |
| **LOGIN-01**: Login flow uses ProxyManager for Chrome | Phase 23 | ✅ Satisfied | `_run_login_job` passes `--proxy-server=socks5://...` to Chrome via `_proxy_manager.get_working_proxy()` |
| **DETAIL-01**: Product detail uses ProxyManager as primary | Phase 21 | ✅ Satisfied | `pm = _proxy_manager` in product_details endpoint |
| **INFRA-01**: ProxyManager is single gateway | Phase 21 | ✅ Satisfied | Singleton at module level, used in img/detail/cart endpoints |

## Phase Verification Status

| Phase | VERIFICATION.md | SUMMARY.md | VALIDATION.md | Status |
|-------|----------------|------------|---------------|--------|
| 21 | ❌ Missing | ❌ Missing | ❌ Missing | Unverified (code works, docs missing) |
| 22 | N/A (1-line change) | ❌ Missing | N/A | Verified via browser test |
| 23 | N/A (refactor) | ❌ Missing | N/A | Verified via grep (zero SOCKS_PROXY in backend/cart/bot) |

## Integration Check

| From | To | Status | Notes |
|------|-----|--------|-------|
| ProxyManager singleton | `/api/img` | ✅ | Smart CDN routing (cdn→direct, img→proxy) |
| ProxyManager singleton | product_details | ✅ | Uses `pm = _proxy_manager` |
| ProxyManager singleton | VkusVillCart | ✅ | Passed via `proxy_manager=` constructor param |
| ProxyManager singleton | bot/handlers.py | ✅ | Creates own ProxyManager instance for cart ops |
| ProxyManager | login Chrome | ✅ | `--proxy-server=socks5://{proxy}` in `_run_login_job` |
| ProxyManager | scrapers | ⚠️ Tech debt | Scrapers still have 3 SOCKS_PROXY fallbacks |

## ✅ LOGIN-01 Fixed (2026-04-01)

Chrome login in `_run_login_job()` now passes `--proxy-server=socks5://{proxy}` using `_proxy_manager.get_working_proxy()`. Standalone `login.py` was deleted (unused).

## Tech Debt Summary

- **3 SOCKS_PROXY references** in scrapers (utils.py, scrape_green.py, green_common.py) — these are fallbacks alongside ProxyManager, low priority
- **Missing planning artifacts** — no VERIFICATION.md, SUMMARY.md, or VALIDATION.md for any v1.4 phase
- **Nyquist compliance**: MISSING for all phases

## Recommendation

**Status: `gaps_found`** — LOGIN-01 is unsatisfied. Options:
1. Implement LOGIN-01 (add ProxyManager to login.py Chrome launch)
2. Defer LOGIN-01 to future milestone and accept gap
3. Remove LOGIN-01 from v1.4 scope (it was aspirational)
